/** @type {import('next').NextConfig} */
// Build v13 - Fixed nested buttons and API response handling
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
}

export default nextConfig
